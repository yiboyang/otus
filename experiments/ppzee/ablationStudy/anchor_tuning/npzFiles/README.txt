This directory is where make_npz.ipynb will place the generated npz files.
These npz files are too large to be uploaded to GitHub but can easily be generated by running make_npz.ipynb for each desired choice of hyperparameter.